import { Button } from '@/components/ui/button';
import { HelpCircle } from 'lucide-react';

export function InstructionToggle() {
  // Component is now disabled - return null to hide it completely
  return null;
}